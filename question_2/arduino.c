#include "arduino.h"


void delay(unsigned int milliseconds)
{
    /* Specific Arduino implementation. */
}

bool digitalRead(int pin)
{
    /* Specific Arduino implementation. */
}

void pinMode(int pin, int mode)
{
    /* Specific Arduino implementation. */
}
